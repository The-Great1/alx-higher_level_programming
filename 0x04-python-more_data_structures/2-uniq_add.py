#!/usr/bin/python3

def uniq_add(my_list=[]):
    my_set = set(my_list)
    # set function here sorts out unique values that is it

    #  removes duplicate values
    return sum(my_set)  # sum to add the values before returning
